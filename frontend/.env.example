# Points the dashboard at your FastAPI backend.
# Local dev: leave as-is (matches `python main.py` default port).
# Production: set to your deployed Render backend URL, e.g.
#   VITE_API_URL=https://payment-recovery-backend.onrender.com
VITE_API_URL=http://localhost:8000
